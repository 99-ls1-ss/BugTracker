@using System.Collections
@using BugTracker.Models
@model ProjectUsersModel

@{
    ViewBag.Menu = "Projects";
    ViewBag.Message = "Edit Project Users";
    ViewBag.Title = "Edit Project Users";
}

<div class="container">
    <div class="wrapper-2">
        <h4><span>@ViewBag.Message for the @Html.DisplayFor(m => m.Project.Name) Project</span></h4>
        <div class="row">
            <div class="col-md-9">
                @Html.ActionLink("Back to Project List", "Index", null, new { @class = "text-right" })
            </div>
            <div class="col-md-3 text-right">
                @if (User.IsInRole("Admin")) {
                    @Html.ActionLink("Create New Project", "Create")
                }
            </div>
        </div>
    </div>
</div>
<div class="container">
    <div class="wrapper-2">
        <div class="row">
            <div id="site_content">
                <div class="col-md-offset-3 col-md-6">
                    <form method="post" action="@Url.Action("Edit")">

                        @Html.AntiForgeryToken()
                        @Html.HiddenFor(m => Model.Project.Id)

                        @Html.ListBoxFor(m => m.SelectedUsers, Model.Users, new { @class = "form-control" })

                        <br />
                        <button class="btn btn-flat flat-color" type="submit">Submit</button>

                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@*@using (Html.BeginForm("Edit", "Projects", FormMethod.Post)) {

        @Html.AntiForgeryToken()

        <div class="form-horizontal">
            <h4>Edit Projects</h4>
            <hr />
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            @Html.HiddenFor(m => m.)
            @Html.HiddenFor(m => m.Name)

            <div class="form-group">
                @Html.LabelFor(m => m.Name, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.DisplayFor(m => m.Name, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(m => m.Id, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(m => m.Users, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.ListBoxFor(m => m.SelectedUsers, Model.Projects)

                    @Html.ListBoxFor(m => m.Users, allUsers, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.Users, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Save" class="btn btn-flat flat-color" />
                </div>
            </div>
        </div>
    }*@


